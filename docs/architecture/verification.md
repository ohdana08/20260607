# 검증 결과 — 2026-09-17

작업본: `/Users/jinjoopwer/ddakfit-operations-20260917`  
브랜치: `codex/ddakfit-operations-20260917`  
기준 커밋: `e2ee1f1` 및 원본의 진행 중인 수정 22개를 보존한 상태.

## 완료한 검사

| 검사 | 결과·범위 |
| --- | --- |
| `npm ci --ignore-scripts` | lockfile 기준 설치 성공. 새 앱 의존성 없음 |
| `npm run test:operations` | **37/37 통과**. 날짜·매출·미확인·누적값 대체·권한·입력 크기·Origin·충돌·오류·로컬 파일 영속화 |
| `npm run test:guards` | 기존 진입·제출유형·수집원·근거·모의심사·사실기반 수정·발표 결제·문서 회귀 검사 통과 |
| `npx tsc --noEmit` | 통과 |
| 신규 경로 ESLint | `app/operations`, `app/api/operations`, `lib/operations`, 신규 테스트·실행 스크립트 통과 |
| `npm run build` | production build 통과. `/operations` 및 `/api/operations` 서버 경로 포함 |
| 계약 파일 | JSON 파싱 및 로컬 `$ref` 20개 해결 확인. 범용 OpenAPI 검증기를 실행한 결과는 아님 |
| 기존 파일 보존 | 원본 수정 22개와 작업본에 복사한 22개의 SHA-256이 시작 시점과 모두 일치 |

Node 내장 테스트 실행 시 TypeScript 변환 기능 및 모듈 형식 감지 경고가 출력된다. 테스트 실패가 아니며 런타임 의존성 변경으로 경고를 숨기지 않았다.

## 실제 브라우저 확인

Chrome에서 `http://127.0.0.1:3107/operations`를 열고 다음 흐름을 확인했다.

1. 초기 미기록 상태가 매출 0원으로 조작되지 않고 미확인으로 표시됨.
2. 사용자 보고에 따라 2026-09-17 현재 성과 0을 저장.
3. 목표 1,000,000원, 남은 Word 주문 34건, 남은 14일, 하루 필요 매출 71,429원 표시.
4. 페이지 새로고침 후 저장된 수치와 메모가 유지됨.
5. 첫 영상 주제를 계획 상태로 추가하고 제목 수정. 월 문서 안에 중복 없이 1개 유지, revision 증가.
6. 1200px 데스크톱과 390px 모바일 화면에서 목표 카드·월 선택·입력 배치 확인.

초기 브라우저 저장에서 개발 서버의 `localhost` URL 정규화로 동일 출처 판정이 실패했다. 실제 Host와 Origin을 대조하도록 수정한 뒤 저장에 성공했고 회귀 검사를 추가했다. 정규화된 URL만으로 원격 Host가 로컬 권한을 얻을 수 없도록 별도 검증했다.

남겨 둔 로컬 기록은 사용자 보고 성과 0과 첫 영상 **계획**이다. 영상 게시나 실제 주문이 발생한 것으로 기록하지 않았다. 파일은 `.local/operations/2026-09.json`에 있으며 Git에서 제외된다.

## 아직 검증하지 않은 운영 항목

- 실제 Google 관리자 로그인·일반 계정 접근 차단을 배포된 화면에서 수행하는 검사. 자동 검사에서는 인증 의존성의 테스트 대역을 사용했다.
- 실제 Upstash Redis EVAL 실행, 여러 서버 인스턴스의 동시 변경, 백업 복구. 로컬 파일의 동시 저장은 실제 검증했고 Redis Lua는 코드와 원자적 저장 설계 수준으로 확인했다.
- 전체 기존 제품의 실결제·환불·유료 AI 생성 재실행. 기존 가드 통과를 실결제 E2E 완료로 해석하지 않는다.
- 부하 p95, 운영 RPO/RTO 달성 여부. 설계 목표이며 측정 결과가 아니다.
- YouTube/GA4·실주문 매출 자동 수집. 아직 구현하지 않은 확장이다.

`npm audit`에는 기존 `image-size` 및 상위 `pptxgenjs` 경로의 high 경고 2건이 남아 있다. 자동 수정 제안이 발표자료 라이브러리의 호환성을 깨는 다운그레이드를 포함해 적용하지 않았다. 이번 모듈에서 두 라이브러리를 호출하지 않지만 전체 서비스 배포 검토에서 별도로 해결해야 한다.

운영 도메인 배포, 운영 DB·환경변수 변경, 실제 결제·고객 발송은 수행하지 않았다. [배포 안내](runbook.md)에 따라 Preview에서 실인증·별도 Redis를 검증한 뒤 운영 반영할 수 있다.
